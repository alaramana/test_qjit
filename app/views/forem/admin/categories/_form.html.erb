<%= simple_form_for [forem, :admin, @category] do |f| %>
  <%= f.input :name %>
  <%= f.submit :class => "btn btn-primary" %>

  <%=link_to "Cancel", forem.admin_categories_path, :class => "btn" %>

<% end %>

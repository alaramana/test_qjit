=form_for [:admin, @medical_organization],:validate => true,:html => { :class => 'form-horizontal medical-org'} do |f|
  .control-group
    %label.control-label.required Name:
    = f.text_field :name, :maxlength => 50, :class => 'input-xlarge'
  .school-form
    = f.submit "Save", :class => 'btn btn-primary'
    = link_to "Cancel",  admin_medical_organizations_path, :class => 'btn'

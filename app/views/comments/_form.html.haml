/ = simple_form_for @comment,:url => { :controller => :comments, :action => :create ,:id =>params[:id]}, :html => { :class => 'form-horizontal' } do |f|
/   = f.input :title,  :input_html=>{:rows=>"2"}
/   %br/
/   = f.input :comment, :input_html=>{:rows=>"2"}
/   %br/
/   .offset2.actions
/     = f.button :submit, :class => 'btn btn-primary'
/     = link_to t('.cancel', :default => t("helpers.links.cancel")),medical_cases_path, :class => 'btn'                               |

%hr/
.span12
  - @comments.each do |comment|
    %h4= comment.title
    %span
      %strong Author :
      =comment.user.full_name unless comment.user.nil?
    %span.pull-right
      %strong Create at :
      = comment.created_at

    %p= comment.comment
    %hr/


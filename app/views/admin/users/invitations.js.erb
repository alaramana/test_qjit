$("#invitations").append("<%= j render('invitations')%>");
<% unless @invitations.last_page? %>
  $(".pagination").replaceWith("<%= j paginate @invitations %>");
<% else %>
  $('.pagination').remove();
<% end %>